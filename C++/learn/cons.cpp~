#include <iostream>
#include <cstdlib>
#include <cstdio>
class A
{
private:
  int x, y;
public:
  A(int x, int y);
  A(int x1) : x(x1), y(0){}
  A() : x(0), y(0){}
};
A::A(int x1, int y1){x = x1; y = y1;}
int main(int argc, char *argv[])
{
  A a;
  return 0;
}
