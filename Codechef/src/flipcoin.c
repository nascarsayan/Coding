#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[])
{
  int n, t, s = 0, *arr;
  scanf("%d %d%*c", &n, &t);
  arr = (int*) calloc(n, sizeof(int));
  while(t--)
    {
      scanf(" ")
    }
  return 0;
}
